name: Java CI

on: [push]

jobs:
  build:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v3
      - name: install putty
        run: |
          choco install putty.install --version=0.71
          C:
          cd 'Program Files'
          ls
          cd PuTTY
          ls
          
        
      
