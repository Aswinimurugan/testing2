name: Java CI

on: [push]

jobs:
  build:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v3
      - name: install putty
        run: |
          C:
          cd 'Program Files'
          ls
          cd PuTTY
          ls
          
        
      
